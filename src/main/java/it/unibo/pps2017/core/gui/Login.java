package it.unibo.pps2017.core.gui;

public interface Login {

    /**
     * Method that handles the login function, which passes the username and password field to
     * client controller in way to check if the credentials are correct
     */
    void checkLogin();

}
