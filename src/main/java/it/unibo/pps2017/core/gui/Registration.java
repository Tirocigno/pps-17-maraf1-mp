package it.unibo.pps2017.core.gui;

public interface Registration {

    /**
     * Method that handles the registration function, which passes the username and password field to
     * client controller in way to check if the new credentials are correct
     */
    void registerUser();

}
